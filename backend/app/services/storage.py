"""""""File upload service — Supabase Storage integration.""""""""    Handles uploading files to Supabase Storage and returning public URLs.    Uses the Supabase Python client's storage API.    Setup required:      1. In Supabase Dashboard → Storage → Create bucket named "evidence"      2. Set bucket to public (or configure RLS policies)      3. No extra env vars needed — uses existing SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY"""""""from __future__ import annotationsimport loggingimport uuidfrom pathlib import Pathimport loggingfrom app.core.config import supabaselogger = logging.getLogger(__name__)BUCKET_NAME = "evidence"